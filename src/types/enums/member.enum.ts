export enum MemberType {
  COMPANY = "COMPANY",
  CANDIDATE = "CANDIDATE",
  ADMIN = "ADMIN",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

export enum MemberFeatured {
  YES = "YES",
  NO = "NO",
}

export enum MemberSort {
  createdAt = "createdAt",
  memberViews = "memberViews",
}
